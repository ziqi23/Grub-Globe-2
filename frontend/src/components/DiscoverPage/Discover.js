import { useSelector } from "react-redux";

const Discover = props => {
    const user = useSelector(session.user);
    favoriteRecipeIds = user.favorites;
    return (
        <>
        </>

    );
};

export default Discover;